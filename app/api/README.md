# API folder

This folder will be used as an API to the StreamTrigger project.
In case of multiple major API versions, it will be splitted into multiple routes.
Each version of the plugin will have a corresponding API version working with it.
